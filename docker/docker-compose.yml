services:
  app:
    build: .
    container_name: hostel_saas_app
    ports:
      - "8080:8080"
    env_file:
      - ../.env
    depends_on:
      - rabbitmq

  rabbitmq:
    image: rabbitmq:3-management
    container_name: hostel_rabbitmq
    ports:
      - "5672:5672"
      - "15672:15672"